args:
  create:
    - python=3.12
    - node=24
    - psql
    - podman
    - claude
    - claude-credentials

port-forward:
  - 3100:3100
  - 8000:8000
  - 8010:8010

builder:
  additional-run:
    - sudo apt-get update && sudo apt-get install -y --no-install-recommends pkg-config libicu-dev libvips-dev libmagic-dev libmagic-mgc tzdata && sudo rm -rf /var/lib/apt/lists/*
    - sudo bash -c 'arch=$(uname -m); case $arch in x86_64) arch=amd64;; aarch64) arch=arm64;; esac; rm -rf /usr/local/go && curl -fsSL "https://go.dev/dl/go1.26.4.linux-${arch}.tar.gz" | tar -C /usr/local -xz'
    - echo 'export PATH="/usr/local/go/bin:$PATH"' >> /home/user/.profile
