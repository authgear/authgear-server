query ResourceQuery($id: ID!) {
  node(id: $id) {
    ... on Resource {
      id
      name
      resourceURI
      clientIDs
      accessPolicy {
        allowDynamicThirdPartyClientAccess
      }
      createdAt
      updatedAt
    }
  }
}
