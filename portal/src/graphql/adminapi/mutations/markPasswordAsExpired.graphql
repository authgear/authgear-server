mutation markPasswordAsExpired(
  $userID: ID!
  $isExpired: Boolean!
) {
  markPasswordAsExpired(
    input: {
      userID: $userID
      isExpired: $isExpired
    }
  ) {
    user {
      id
    }
  }
}
