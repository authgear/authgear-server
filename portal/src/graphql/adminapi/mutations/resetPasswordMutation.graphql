mutation resetPasswordMutation($userID: ID!, $password: String!, $sendPassword: Boolean, $forceChangeOnLogin: Boolean) {
  resetPassword(input: { userID: $userID, password: $password, sendPassword: $sendPassword, forceChangeOnLogin: $forceChangeOnLogin }) {
    user {
      id
    }
  }
}
