query nftContractMetadataQuery($contractID: String!) {
  nftContractMetadata(contractID: $contractID) {
    name
    blockchain
    network
    contractAddress
    totalSupply
    tokenType
    createdAt
  }
}
