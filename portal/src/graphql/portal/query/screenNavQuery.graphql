query screenNavQuery($id: ID!) {
  node(id: $id) {
    __typename
    ... on App {
      id
      effectiveFeatureConfig
      planName
      tutorialStatus {
        appID
        data
      }
    }
  }
}
