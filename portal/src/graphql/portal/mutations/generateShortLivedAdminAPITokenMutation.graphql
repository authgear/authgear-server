mutation generateShortLivedAdminAPIToken(
  $appID: ID!
  $appSecretVisitToken: String!
) {
  generateShortLivedAdminAPIToken(
    input: { appID: $appID, appSecretVisitToken: $appSecretVisitToken }
  ) {
    token
  }
}
