mutation watchNFTCollectionsMutation(
  $appID: String!
  $contractIDs: [String!]!
) {
  watchNFTCollections(input: { id: $appID, contractIDs: $contractIDs }) {
    app {
      id
      nftCollections {
        name
        blockchain
        network
        contractAddress
        totalSupply
        tokenType
        createdAt
      }
    }
  }
}
