{{ if eq .UsageAction "alert" }}Penggunaan {{ .UsageDisplayName }} Anda telah mencapai {{ .UsageCurrentValue }} ({{ template "app.name" }}){{ else if eq .UsageAction "block" }}Penggunaan {{ .UsageDisplayName }} telah diblokir{{ else }}Penggunaan {{ .UsageDisplayName }} Anda telah mencapai {{ .UsageCurrentValue }} ({{ template "app.name" }}){{ end }}

{{ if eq .UsageAction "alert" }}Halo,

Proyek Authgear Anda, {{ template "app.name" }}, telah menggunakan {{ .UsageCurrentValue }} dari kuota {{ .UsageDisplayName }} untuk periode penagihan ini.

Tidak ada tindakan yang diperlukan saat ini. Ini hanya pemberitahuan agar Anda dapat meninjau penggunaan Anda.

Jika Anda memiliki pertanyaan, silakan hubungi kami.

Terbaik,
Tim Authgear{{ else if eq .UsageAction "block" }}Halo,

Proyek Anda, {{ template "app.name" }}, telah mencapai batas keras {{ .UsageQuota }} {{ .UsageDisplayName }} dalam periode penagihan ini. Penggunaan {{ .UsageDisplayName }} tambahan telah diblokir.

Untuk melanjutkan layanan, silakan tingkatkan paket Anda.

Jika Anda memiliki pertanyaan, silakan hubungi kami.

Terbaik,
Tim Authgear{{ else }}Halo,

Proyek Authgear Anda, {{ template "app.name" }}, telah menggunakan {{ .UsageCurrentValue }} dari kuota {{ .UsageDisplayName }} untuk periode penagihan ini.

Tidak ada tindakan yang diperlukan saat ini. Ini hanya pemberitahuan agar Anda dapat meninjau penggunaan Anda.

Jika Anda memiliki pertanyaan, silakan hubungi kami.

Terbaik,
Tim Authgear{{ end }}
