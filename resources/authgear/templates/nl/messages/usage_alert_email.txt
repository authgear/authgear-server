{{ if eq .UsageAction "alert" }}Uw {{ .UsageDisplayName }} gebruik heeft {{ .UsageCurrentValue }} bereikt ({{ .AppID }}){{ else if eq .UsageAction "block" }}{{ .UsageDisplayName }} gebruik is geblokkeerd{{ else }}Uw {{ .UsageDisplayName }} gebruik heeft {{ .UsageCurrentValue }} bereikt ({{ .AppID }}){{ end }}

{{ if eq .UsageAction "alert" }}Hallo,

Uw Authgear-project, {{ .AppID }}, heeft {{ .UsageCurrentValue }} van uw {{ .UsageDisplayName }} quotum voor deze factureringsperiode gebruikt.

Er is op dit moment geen actie nodig. Dit is slechts een waarschuwing zodat u uw gebruik kunt controleren.

Als u vragen hebt, neem dan contact met ons op.

Met vriendelijke groeten,
Authgear Team{{ else if eq .UsageAction "block" }}Hallo,

Uw project, {{ .AppID }}, heeft de harde limiet van {{ .UsageQuota }} {{ .UsageDisplayName }} in deze factureringsperiode bereikt. Aanvullend {{ .UsageDisplayName }} gebruik is geblokkeerd.

Om de service te hervatten, upgrade uw plan.

Als u vragen hebt, neem dan contact met ons op.

Met vriendelijke groeten,
Authgear Team{{ else }}Hallo,

Uw Authgear-project, {{ .AppID }}, heeft {{ .UsageCurrentValue }} van uw {{ .UsageDisplayName }} quotum voor deze factureringsperiode gebruikt.

Er is op dit moment geen actie nodig. Dit is slechts een waarschuwing zodat u uw gebruik kunt controleren.

Als u vragen hebt, neem dan contact met ons op.

Met vriendelijke groeten,
Authgear Team{{ end }}
