{{ if eq .UsageAction "alert" }}Está a aproximar-se do seu limite de utilização de {{ .UsageDisplayName }}{{ else if eq .UsageAction "block" }}A utilização de {{ .UsageDisplayName }} foi bloqueada{{ else }}Está a aproximar-se do seu limite de utilização de {{ .UsageDisplayName }}{{ end }}

{{ if eq .UsageAction "alert" }}Olá,

O seu projeto Authgear, {{ template "app.name" }}, utilizou {{ .UsageCurrentValue }} quotas de {{ .UsageDisplayName }} neste período de faturação.

Pode continuar por enquanto. Para evitar qualquer interrupção, considere atualizar o seu plano.

Se tiver alguma dúvida, contacte-nos.

Cumprimentos,
Equipa Authgear{{ else if eq .UsageAction "block" }}Olá,

O seu projeto, {{ template "app.name" }}, atingiu o seu limite máximo de {{ .UsageQuota }} {{ .UsageDisplayName }} neste período de faturação. A utilização adicional de {{ .UsageDisplayName }} foi bloqueada.

Para retomar o serviço, atualize o seu plano.

Se tiver alguma dúvida, contacte-nos.

Cumprimentos,
Equipa Authgear{{ else }}Olá,

O seu projeto Authgear, {{ template "app.name" }}, utilizou {{ .UsageCurrentValue }} quotas de {{ .UsageDisplayName }} neste período de faturação.

Pode continuar por enquanto. Para evitar qualquer interrupção, considere atualizar o seu plano.

Se tiver alguma dúvida, contacte-nos.

Cumprimentos,
Equipa Authgear{{ end }}
