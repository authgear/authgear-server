name: Signup with email password and email OTP 2FA via webapp
authgear.yaml:
  override: |
    authentication:
      identities:
      - login_id
      primary_authenticators:
      - password
      secondary_authenticators:
      - oob_otp_email
      secondary_authentication_mode: required
    identity:
      login_id:
        keys:
        - key: email
          type: email
    verification:
      claims:
        email:
          enabled: true
          required: false
        phone_number:
          enabled: true
          required: true
    authenticator:
      oob_otp:
        email:
          email_otp_mode: code
steps:
  - name: oauth_setup
    action: oauth_setup
  - name: get_signup_page
    action: http_request
    http_request_method: GET
    http_request_url: http://127.0.0.1:4000/signup?{{ .steps.oauth_setup.result.query }}&q_login_id_key=email&q_login_id_input_type=email
    http_output:
      http_status: 200
      redirect_path: /signup
  - name: submit_signup_id
    action: http_request
    http_request_method: POST
    http_request_url: "{{ .steps.get_signup_page.result.http_final_url }}"
    http_request_form_urlencoded_body:
      q_login_id_key: email
      q_login_id: webapp-signup-2fa@example.com
      x_action: login_id
    http_output:
      http_status: 200
      redirect_path: /authflow/v2/create_password
      html_xpath_exists:
        - /html/body//input[@name='x_password']
  - name: submit_password
    action: http_request
    http_request_method: POST
    http_request_url: "{{ .steps.submit_signup_id.result.http_final_url }}"
    http_request_form_urlencoded_body:
      x_password: password
      x_confirm_password: password
    http_output:
      http_status: 200
      redirect_path: /authflow/v2/setup_oob_otp
      html_xpath_exists:
        - /html/body//input[@name='x_target']
  - name: submit_2fa_target
    action: http_request
    http_request_method: POST
    http_request_url: "{{ .steps.submit_password.result.http_final_url }}"
    http_request_form_urlencoded_body:
      x_target: webapp-signup-2fa-mfa@example.com
    http_output:
      http_status: 200
      redirect_path: /authflow/v2/enter_oob_otp
      html_xpath_exists:
        - /html/body//input[@name='x_code']
  - name: submit_code
    action: http_request
    http_request_method: POST
    http_request_url: "{{ .steps.submit_2fa_target.result.http_final_url }}"
    http_request_form_urlencoded_body:
      x_code: "111111"
      x_action: submit
    http_output:
      http_status: 200
      redirect_path: /authflow/v2/view_recovery_code
      html_xpath_exists:
        - /html/body//form//button[@value='proceed']
  - name: confirm_recovery_code
    action: http_request
    http_request_method: POST
    http_request_url: "{{ .steps.submit_code.result.http_final_url }}"
    http_request_follow_redirects: false
    http_request_form_urlencoded_body:
      x_action: proceed
    http_output:
      http_status: 302
  - name: finish_flow
    action: http_request
    http_request_method: GET
    http_request_url: "http://127.0.0.1:4000{{ .steps.confirm_recovery_code.result.http_response_headers.location }}"
    http_request_follow_redirects: false
    http_output:
      http_status: 302
  - action: oauth_exchange_code
    oauth_exchange_code_code_verifier: "{{ .steps.oauth_setup.result.code_verifier }}"
    oauth_exchange_code_redirect_uri: "{{ .steps.finish_flow.result.http_response_headers.location }}"
    output:
      result: |
        {
          "id_token": {
            "sub": "[[string]]"
          }
        }
  - action: query
    query: |
      SELECT id
      FROM _auth_user
      WHERE app_id = '{{ .AppID }}'
      AND standard_attributes ->> 'email' = 'webapp-signup-2fa@example.com';
    query_output:
      rows: |
        [
          {
            "id": "[[string]]"
          }
        ]
