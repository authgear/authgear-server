package interaction

import (
	"github.com/skygeario/skygear-server/pkg/auth/dependency/auth"
)

func (p *Provider) NewInteraction(intent Intent, session auth.AuthSession) (*Interaction, error) {
	// TODO(interaction): do something
	return nil, nil
}
